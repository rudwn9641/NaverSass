{
  "scripts": {
    "start" : "run-p watch dev",
    "build": "sass scss:css",
    "watch": "npm run build -- --watch",
    "dev": "live-server --port=8090"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5",
    "sass": "^1.53.0"
  }
}
